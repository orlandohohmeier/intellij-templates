//
// AMD Intellij Template
// @author Orlando Hohmeier (orlandohohmeier.com)
//
define(function (require) {
    "use strict";

    //#import

    //@implementation

    /**
     * ${Name}
     *
     * @name ${Name}
     * @author ${USER}
     *
     * @constructor
     */
    function ${Name}() {

        /**
         * Init
         */
        function init() {

        }

        //Call Init
        init.call(this);
    }

    //Export
    return ${Name};

    //@end
});